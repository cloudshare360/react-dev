const z = 10;
console.log('Initial value:', z); // Output: 10

// Attempting to reassign a value to a const variable will result in a runtime error.
// TypeError: Assignment to constant variable.
// z = 20; one can't re-assigned the value

